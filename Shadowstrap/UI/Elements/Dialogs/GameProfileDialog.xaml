<base:WpfUiWindow x:Class="Shadowstrap.UI.Elements.Dialogs.GameProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:base="clr-namespace:Shadowstrap.UI.Elements.Base"
        mc:Ignorable="d"
        Title="Game Profile"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ExtendsContentIntoTitleBar="True">

    <Grid Margin="0,32,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="20,0,20,0">

            <!-- Game Name -->
            <TextBlock Text="Game name" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
            <ui:TextBox x:Name="NameBox" PlaceholderText="e.g. The Strongest Battlegrounds"
                        Margin="0,0,0,14" />

            <!-- Place ID -->
            <TextBlock Text="Place ID" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" />
            <Grid Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox x:Name="PlaceIdBox" Grid.Column="0"
                            PlaceholderText="Numeric place ID" />
                <ui:Button x:Name="LookupBtn" Grid.Column="2"
                           Content="Look up" Click="LookupBtn_Click" />
            </Grid>

            <!-- Flags JSON -->
            <TextBlock FontSize="12" Margin="0,0,0,4"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                <Run Text="Flags" />
                <Run Text=" (JSON  { &quot;FlagName&quot;: &quot;Value&quot;, … })"
                     Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="11" />
            </TextBlock>
            <TextBox x:Name="FlagsBox"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     Height="180" FontFamily="Consolas, monospace" FontSize="12"
                     VerticalScrollBarVisibility="Auto"
                     Padding="8" Margin="0,0,0,8"
                     Background="{DynamicResource ControlFillColorDefaultBrush}"
                     BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                     BorderThickness="1" />

            <TextBlock x:Name="ErrorText" Foreground="#EF4444" FontSize="12"
                       TextWrapping="Wrap" Margin="0,0,0,8" Visibility="Collapsed" />
        </StackPanel>

        <!-- Footer -->
        <Border Grid.Row="1" Padding="20,12" Margin="0,12,0,0"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Cancel" Click="Cancel_Click" Margin="0,0,8,0" />
                <ui:Button Content="Save" Appearance="Primary" Click="Save_Click" />
            </StackPanel>
        </Border>
    </Grid>
</base:WpfUiWindow>
