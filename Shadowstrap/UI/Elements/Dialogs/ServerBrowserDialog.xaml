<base:WpfUiWindow x:Class="Shadowstrap.UI.Elements.Dialogs.ServerBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:base="clr-namespace:Shadowstrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:Shadowstrap.UI.ViewModels"
        d:DataContext="{d:DesignInstance vm:ServerBrowserViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="Server Browser"
        Width="680"
        Height="560"
        MinWidth="520"
        MinHeight="400"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"    />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <ui:TitleBar Grid.Row="0" Padding="8" Title="Server Browser"
                     ShowMinimize="False" ShowMaximize="False" CanMaximize="False"
                     KeyboardNavigation.TabNavigation="None"
                     Icon="pack://application:,,,/Shadowstrap.ico" />

        <!-- Search row -->
        <Border Grid.Row="1" Padding="14,10"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:TextBox Grid.Column="0"
                            PlaceholderText="Place ID  (e.g. 155615529 for Natural Disaster Survival)"
                            Text="{Binding PlaceIdText, UpdateSourceTrigger=PropertyChanged}"
                            Margin="0,0,8,0">
                    <ui:TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding BrowseCommand}" />
                    </ui:TextBox.InputBindings>
                </ui:TextBox>

                <ui:Button Grid.Column="1" Content="Browse" Icon="Search24"
                           Command="{Binding BrowseCommand}"
                           IsEnabled="{Binding IsNotLoading}"
                           Margin="0,0,10,0" />

                <CheckBox Grid.Column="2" Content="Hide full"
                          IsChecked="{Binding HideFull}"
                          VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Error strip -->
        <Border Grid.Row="2" Padding="14,8,14,0">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasError}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" FontSize="12" TextWrapping="Wrap" />
        </Border>

        <!-- Game info card -->
        <Border Grid.Row="3" Margin="14,8,14,0" CornerRadius="6"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1"
                Padding="12,10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasGameInfo}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <Border Width="44" Height="44" CornerRadius="4" Margin="0,0,12,0">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding GameThumb, IsAsync=True}"
                                    Stretch="UniformToFill" />
                    </Border.Background>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding GameName}" FontSize="14" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding GamePlaying}" FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,2,0,0" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Server list + loading overlay -->
        <Grid Grid.Row="4" Margin="0,8,0,0">

            <!-- Loading spinner -->
            <ui:ProgressRing IsIndeterminate="True" Width="42" Height="42"
                             HorizontalAlignment="Center" VerticalAlignment="Center">
                <ui:ProgressRing.Style>
                    <Style TargetType="ui:ProgressRing">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:ProgressRing.Style>
            </ui:ProgressRing>

            <!-- Server ListView -->
            <ListView ItemsSource="{Binding Servers}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      BorderThickness="0">
                <ListView.Style>
                    <Style TargetType="ListView" BasedOn="{StaticResource {x:Type ListView}}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsLoading}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Players" Width="90"
                                        DisplayMemberBinding="{Binding PlayerDisplay}" />
                        <GridViewColumn Header="Ping"    Width="80"
                                        DisplayMemberBinding="{Binding PingDisplay}" />
                        <GridViewColumn Header="FPS"     Width="60"
                                        DisplayMemberBinding="{Binding FpsDisplay}" />
                        <GridViewColumn Header="Region"  Width="150"
                                        DisplayMemberBinding="{Binding Region}" />
                        <GridViewColumn Header="" Width="76">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <ui:Button Content="Join" Padding="10,4"
                                               Command="{Binding DataContext.JoinCommand,
                                                         RelativeSource={RelativeSource AncestorType=ListView}}"
                                               CommandParameter="{Binding}"
                                               Appearance="Success"
                                               Foreground="White" IconForeground="White" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="5" Padding="14,10"
                BorderThickness="0,1,0,0"
                BorderBrush="{DynamicResource ControlElevationBorderBrush}">
            <Grid>
                <TextBlock Text="{Binding ServerCount}" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           FontSize="12" />
                <ui:Button HorizontalAlignment="Right" Content="Load More" Icon="ArrowDown24"
                           Command="{Binding LoadMoreCommand}"
                           IsEnabled="{Binding CanLoadMore}" />
            </Grid>
        </Border>
    </Grid>
</base:WpfUiWindow>
